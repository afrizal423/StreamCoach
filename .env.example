# APP_ENV: local or production
APP_ENV=local

# Redis Configuration (Required if APP_ENV=production)
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# Concurrency Limit (Max active analysis tasks)
MAX_CONCURRENT_TASKS=2
